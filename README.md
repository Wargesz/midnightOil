# Midnight Oil
vim plugin for a self hosted wakatime clone service

The config file should be in your home directory named: `.midnightOil.config`, with the following lines:
```
address=<url-to-target>
api-key=<generated-key>
```
